<!--index.wxml-->
<view class="row">
    <navigator wx:for="{{items}}" data-name="{{item.text}}" bindtap="choose">
        <view class="item_size">
            <image class="img" src="{{item.src}}"></image>
            <text class="text">{{item.text}}</text>
        </view>
    </navigator>
</view>